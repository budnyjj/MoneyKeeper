APP_ABI               := armeabi-v7a
APP_MODULES           := cv
APP_OPTIM             := release
APP_PLATFORM          := android-15
APP_STL               := c++_shared
NDK_TOOLCHAIN_VERSION := clang
